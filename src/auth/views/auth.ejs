<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/css/Global.css">
    <link rel="stylesheet" href="/css/Index.css">
    <link rel="icon" type="image/png" href="/images/logo_docsigned.png">
    <title>NewStetic - DocSigned</title>
</head>
<body>
    <div class="login-container">
        <form id="login-form" action="/api/auth/login" method="POST">
            <!-- Aquí se mostrarán los errores de validación -->
        <% if (typeof error !== 'undefined') { %>
            <div class="alert alert-danger">
                <%= error %>
            </div>
        <% } %>
            <!-- Header del formulario -->
            <div class="login-header">
                <div class="login-logo">DS</div>
                <h1 class="login-title">Bienvenido</h1>
                <p class="login-subtitle">Ingresa tu usuario y contraseña para acceder a DocSigned</p>
            </div>
            
            <!-- Campo de usuario -->
            <div class="form-group">
                <label class="form-label" for="username">Usuario</label>
                <input 
                    type="text" 
                    id="username"
                    name="username"
                    class="form-input" 
                    placeholder="Tu usuario de red"
                    required
                >
                <div class="error-message" style="display: none;">
                    ⚠️ Por favor ingresa tu usuario de red
                </div>
            </div>
            
            <!-- Campo de contraseña -->
            <div class="form-group">
                <label class="form-label" for="password">Contraseña</label>
                <input 
                    type="password" 
                    id="password"
                    name="password"
                    class="form-input" 
                    placeholder="••••••••"
                    required
                >
                <div class="error-message" style="display: none;">
                    ⚠️ La contraseña es obligatoria
                </div>
            </div>
            
            <!-- Botón de login -->
            <button type="submit" class="login-btn">
                Iniciar Sesión
            </button>
        </form>
    </div>
</body>
</html>
